import { Meteor } from 'meteor/meteor';

import '../imports/api/blockchainUtils';
import '../imports/api/sessions';
import '../imports/api/receipts';

Meteor.startup(() => {
  // code to run on server at startup
});
